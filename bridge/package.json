{
  "name": "beacon-bridge",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "bun run src/server.ts",
    "dev": "bun --watch run src/server.ts",
    "stats": "bun run src/cli/stats.ts"
  },
  "dependencies": {
    "hono": "^4.6.14",
    "ccusage": "^15.0.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.7.0"
  }
}
